import ContactForm from "../elements/ContactForm.jsx";

export default function Contact() {

    return (
        <section style={{
            display: "flex",
            flexDirection: "column",
            width: "100%",
        }}>
            <ContactForm/>
        </section>
    )
}